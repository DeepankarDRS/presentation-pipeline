# LLM_TEST_PLAN Family A — base prompt, no components. Planner must decompose.
name: base-org-structure
objective: Engineering org chart — planner should reach for a tree.
request: >
  Show the structure of our engineering organisation — the VP, the directors
  under them, and the teams under each director.
theme: corporate-slate
content_source: LLM invents names, titles and team names
expect:
  compiles: true
  planner_should_pick: [title, tree]
  watch: tree vs bullet fallback; depth <= 3; nodes don't overflow width
