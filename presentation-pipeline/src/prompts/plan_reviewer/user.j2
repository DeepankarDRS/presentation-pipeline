CORE HOOK: {{ core_hook }}

DECK PLAN ({{ slide_plans | length }} slides):

{% for plan in slide_plans %}
--- Slide {{ plan.slide_index + 1 }}: {{ plan.slide_type | upper }} | layout={{ plan.layout_pattern }} | density={{ plan.density }} ---
Components: {{ plan.components | map(attribute='kind') | join(', ') }}

content_data:
```json
{{ plan.content_data | tojson(indent=2) }}
```

{% endfor %}

Review this plan. Identify all issues by type and severity. Return confidence_score, approved, summary, and issues list.
