REQUEST:
{{ raw_request }}
{% if theme_name %}
THEME: {{ theme_name }}
{% endif %}
{% if supplied_content %}
SUPPLIED CONTENT (use these values VERBATIM in content_data — do NOT rephrase or invent alternatives):
```json
{{ supplied_content | tojson(indent=2) }}
```

CONTENT DISTRIBUTION RULES:
- Analyze the supplied keys and assign each to the most appropriate slide.
- KPI keys (kpi_labels, kpi_values, kpi_deltas, kpi_directions) → a data slide with kpi_row component.
- Chart keys (chart_type, chart_title, chart_labels, chart_values, chart_series) → a data slide with chart component.
- Table keys (table_columns, table_rows) → a data slide with table component.
- Bullet keys (bullets) → a content or closing slide with bullet_list component.
- Timeline keys (timeline_items) → a content slide with timeline component.
- Flow keys (flow_steps) → a content slide with flow component.
- Process keys (process_steps) → a content slide with process_arrow component.
- Pyramid keys (pyramid_levels) → a content slide with pyramid component.
- Title/subtitle keys → the cover slide.
- When copying supplied values into a slide's content_data, use the EXACT same key names so provenance tracking works.
- If the request describes which data goes on which slide, follow the request.
- For any slide that needs data NOT in supplied_content, derive plausible values.
{% endif %}
{% if components_hint %}
REQUESTED COMPONENTS: {{ components_hint }}
{% endif %}
{% if audience_context %}
AUDIENCE CONTEXT:
{% for key, value in audience_context.items() %}
- {{ key }}: {{ value }}
{% endfor %}
{% endif %}
{% if target_slides and target_slides > 1 %}
TARGET DECK SIZE: exactly {{ target_slides }} slides. Produce a plan with
{{ target_slides }} slide entries. If the request itself enumerates slides or
names a different count, follow the request — its structure wins.
{% endif %}

Plan the slide(s). You MUST populate content_data with concrete, realistic values for every component. When supplied_content is provided, copy the exact values into the correct slide's content_data using the same key names.