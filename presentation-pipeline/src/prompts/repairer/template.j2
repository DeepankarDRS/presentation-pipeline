{{ previous_user }}

======================  REPAIR — TIER 3 (TEMPLATE)  ======================
Previous attempts failed repeatedly. Use this VERIFIED TEMPLATE as your skeleton.
This template compiles cleanly — do NOT change its structure.

--- VERIFIED TEMPLATE ---
{{ template_xml }}
--- END TEMPLATE ---

INSTRUCTIONS:
1. Use the template above as your exact structural skeleton.
2. Replace ONLY the text content, colors (use $tokens), and data values.
3. Keep the same node hierarchy (VStack/HStack/Text/Shape nesting).
4. Keep all w, h, padding, gap, fontSize values from the template.
5. Apply the Theme element from the system prompt.
6. Return POM XML only — no explanation, no markdown fences.