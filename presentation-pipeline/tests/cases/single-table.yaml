# Phase 5 test case #5. Consumed by slide_ir.from_test_case().
# NOTE: Table/Col/Tr/Td are Phase C nodes (knowledge base + allowlist added in
# Phase 6). The selective contract cannot fully support this case until then.
name: single-table
phase: C
objective: Lay out a segment breakdown as a single clean table.
request: >
  A slide with a bold title and one table breaking revenue down by product
  segment. Columns: Segment, Revenue, QoQ Growth, Share of Total. Four segment
  rows plus a bold total row. Right-align the numeric columns, with a
  subtle header row and hairline row borders.
theme: warm-editorial
components:
  - title
  - table
supplied_content:
  columns: [Segment, Revenue, QoQ Growth, Share of Total]
  segments: [Enterprise, Mid-Market, SMB, Partner]
content_source: Column headers and segment names supplied; figures invented (must sum to the total)
expect:
  compiles: true
  nodes_used: [Slide, Theme, VStack, Text, Table, Col, Tr, Td]
